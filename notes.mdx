## TCP
A reliable protocol that works on top of an unreliable protocol:IP(Internet Protocol)



step 1:
go mod init github.com/karthiknadar1204/social
// This command initializes a new Go module for the project. 
// It creates a go.mod file, which is used to manage dependencies and module versions.
// The module path is set to "github.com/karthiknadar1204/social", which is a unique identifier for the module.
// This step is essential for organizing and managing the project's dependencies in Go.

step 2:
